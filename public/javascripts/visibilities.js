function showOverlay(selector){
    var node = document.querySelector(selector);
    console.log(node);
}

function hideOverlay(selector){
    var node = document.querySelector(selector);
    console.log(node);
}
